#!/bin/bash
OP=$1
PWD=`pwd`
BASE=`basename $PWD`
L="repo=$BASE "
if [ "$OP" == "get" ]; then
	while read CMD; do
		if [ "$CMD" == "" ]; then
			break
		fi
		L="$L $CMD"
	done

	if [ -p $FILE ]; then

		echo -ne "\033]0;$L\007" >/dev/tty 
		exit 0
	fi

fi

exit 1

